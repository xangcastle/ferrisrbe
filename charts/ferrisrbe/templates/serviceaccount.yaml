{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ferrisrbe.serviceAccountName" . }}
  namespace: {{ include "ferrisrbe.namespace" . }}
  labels:
    {{- include "ferrisrbe.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
