{{- if .Values.bazelRemote.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrisrbe.fullname" . }}-bazel-remote
  namespace: {{ include "ferrisrbe.namespace" . }}
  labels:
    app.kubernetes.io/component: bazel-remote
    {{- include "ferrisrbe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.bazelRemote.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
      {{- if and (eq .Values.server.service.type "NodePort") .Values.bazelRemote.service.nodePortGrpc }}
      nodePort: {{ .Values.bazelRemote.service.nodePortGrpc }}
      {{- end }}
    - port: {{ .Values.bazelRemote.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.server.service.type "NodePort") .Values.bazelRemote.service.nodePortHttp }}
      nodePort: {{ .Values.bazelRemote.service.nodePortHttp }}
      {{- end }}
  selector:
    app.kubernetes.io/component: bazel-remote
    {{- include "ferrisrbe.selectorLabels" . | nindent 4 }}
{{- end }}
